# Base URL of the MT Sorella backend API.
# Read in src/lib/config.ts; falls back to "/api" when unset.
# (The exact backend port is agreed with the backend developer — issue #3.)
VITE_API_URL=http://localhost:5080
